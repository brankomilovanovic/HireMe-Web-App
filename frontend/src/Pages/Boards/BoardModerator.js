import React from "react";
import strings from "../../localization";

const BoardModerator = () => {
  return (
    <div style={{maxWidth: '90%', margin: 'auto'}}>
      <h2>{strings.pages.boardModerator.title}</h2>
    </div>
  );
};

export default BoardModerator;
