import React from "react";
import strings from "../../localization";

const BoardUser = () => {
  return (
    <div style={{maxWidth: '90%', margin: 'auto'}}>
      <h2>{strings.pages.boardUser.title}</h2>
    </div>
  );
};

export default BoardUser;
